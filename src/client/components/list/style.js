import styled from "styled-components"

export const ListObject = styled.ul`
  margin: 0;
  list-style: none;
  padding: 0;
`

export const ListInterestStyle = styled(ListObject)``

export default ListObject
